<div class= "container d-flex flex-column mt-4">

<%= form_with(model: [@post, @like], local: true) do |form| %>
  <div class="mb-3">
    <%= form.submit "Save", class: "btn btn-success btn-block" %>
  </div>
<% end %>

</div>